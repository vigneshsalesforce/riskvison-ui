// src/features/pra/praSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const praSlice = createSlice({
  name: 'pra',
  initialState: {},
    reducers: {}
});

export default praSlice.reducer;