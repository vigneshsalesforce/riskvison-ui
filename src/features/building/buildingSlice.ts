// src/features/building/buildingSlice.ts
import { createSlice } from '@reduxjs/toolkit';

const buildingSlice = createSlice({
  name: 'building',
  initialState: {},
    reducers: {}
});

export default buildingSlice.reducer;